; -*- Mode: Emacs-Lisp ; Coding: utf-8 -*-

;; ------------------------------------------------------------------------
;; @ elfeed (rss reader)
(require 'elfeed)
(global-set-key (kbd "C-x w") 'elfeed)
;; ------------------------------------------------------------------------
